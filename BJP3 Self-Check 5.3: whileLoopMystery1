public static void mystery(int x) {
    int y = 1;
    int z = 0;
    while (2 * y <= x) {
        y = y * 2;
        z++;
    }
    System.out.println(y + " " + z);
}


//mystery(1); this will give answer of 1 0;
//mystery(6); 4 2;
//the while loop stops and yield the answer before the test
